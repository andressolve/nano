# Bounded orchestrator

Read project `SESSION-START.md`, `HANDOFF.md`, and disk state. Do not read
production images, old transcripts, or prompts outside the current packet.
Assemble exact script + intent + builder prompt + card into separated
transports; technical-check the candidate; copy candidate/proofs into
`review/current/`; run preflight; dispatch the fresh blind critic; validate and
archive its neutral report; route the JSON history. Promote only approved bytes
with hash verification. One page in flight; event-driven waits; compact receipt.
Stop on invalid report, resistant defect, v4 revision, protected authority, or
milestone gate.
