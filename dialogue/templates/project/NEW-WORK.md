# NEW-WORK

This file governs research and adaptation from the project directory. Read
`../../AUDIENCE.md`, `../../ADAPTATION.md`, this project's `README.md`, current
`HANDOFF.md`, and the smallest relevant source set. Do not load production
manuals or historical project archives unless a bounded question requires
them.

Run fresh, question-bounded research tasks into `research/`. Synthesize their
results before adaptation; do not carry raw transcripts forward. Build the
audience promise, adaptation brief, story architecture, graphical direction,
complete script, and page contract. Use a fresh two-stage audience critic that
sees the story before protected claims and never grades general source
fidelity.

End by saving and directly presenting `adaptation/GREENLIGHT.md` to the owner.
Do not generate references, style explorations, story pages, or prototypes.
After explicit owner approval, run:

```sh
python3 -B ../../tools/check_adaptation.py .
```

Only a clean result permits the production entrypoint in `SESSION-START.md`.
