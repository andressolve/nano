# Bounded reader/publication gate

Verify every promoted story page is present and linked. Test `#page-1` through the final real page, dedicated `#end`, and dedicated `#quiz`; ensure end/quiz cannot be saved as story positions and legacy fake routes normalize. Run quiz interaction, back/catalog routing, local verifier, deployment status, and public URL checks. Do not create a fake story page for end/quiz. Record URLs, commit/deploy identifiers, and a concise receipt.
