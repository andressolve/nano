# Page 23 alternate v7 — Builder Rejection

## Verdict

REJECT — v7 is the first alternate to pass the strict 64 px outer safe-area gate and it preserves every story beat, but P1 ear contact, mouth-local Edmond tails, and uniform native lettering size remain mandatory failures.

## Preserved artifacts

- `page-23-alt-v7.png`: exact 1024 × 1536 RGB source.
- `page-23-alt-v7-desktop.png`: 600 × 900 desktop proof.
- `page-23-alt-v7-tablet.png`: 768 × 1152 tablet proof.
- `page-23-alt-v7-prompt.md`: exact prompt.

## Strict safe-area pass

Connected warm-ivory bounds are:

- P1 `(x=531–930, y=68–257)`;
- P2 Voice `x=68–365`, Edmond `x=754–951`;
- P3 Voice `x=80–374`, Edmond `x=755–943`;
- P4 upper Voice `x=66–371`, Edmond `x=776–954`, lower Voice `x=66–359`.

Every balloon clears 64 px. The raw `scrape` is also safely inset.

## Mandatory failures

- Panel 1 now points Edmond's nose right, away from the left wall, but his exposed ear still sits visibly to the right of the wall edge with hair/air between them. It is not physically pressed or occluded.
- Edmond reply tail tips remain roughly 80–115 px from visible lips in Panels 2–4; they are directional, not local endpoints.
- Type is large and comfortable, but representative line bands still include roughly 34–39 px lines, below the uniform 40 px minimum.

## Passes to preserve

- Exact nine strings/order/case/punctuation; eight balloons plus tail-free `scrape`.
- Five panels, P4 dominant, P5 shortest/silent.
- P2 held crude tool/closed door/Voice tail at seam.
- P3 closed hairline only; P4 first opening and both Voice tails deep in black.
- P5 one living five-digit hand/no owner/Faria/lamp/second figure.
- Safe area, dimensions, RGB, identity/style, desktop/tablet readability.

## Next whole-page correction

Preserve the v7 guide topology but move the P1 stone edge through the ear, extend each reply-tail guide point far left to the speaker's mouth lane, widen Voice fields, and request a uniform 15% type increase.
